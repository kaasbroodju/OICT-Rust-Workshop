fn main() {
    let mut a = 5;
    let mut b = 10;

    println!("Before swap: a = {}, b = {}", a, b);

    // Doormiddel van pointers probeer a, b te swappen
    unsafe {

    }

    println!("After swap: a = {}, b = {}", a, b);
}
